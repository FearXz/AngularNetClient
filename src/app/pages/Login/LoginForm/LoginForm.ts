import { Component } from '@angular/core';

@Component({
  selector: 'LoginForm',
  standalone: true,
  imports: [],
  templateUrl: './LoginForm.html',
})
export class LoginForm {
  title = 'LOGIN';
}
